#include <main.h>

/**
 * positive_or_negative - check for +ve or -ve #
 * @i: the number tested
 * Retun: zero
 */

void positive_or_negative(int i);
{
	if (i < 0)
		pintf("%d is negative\n", i);
	else
		printf("%d is positive\n", i);
}
